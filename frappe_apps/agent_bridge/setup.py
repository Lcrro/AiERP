from setuptools import find_packages, setup


setup(
    name="agent_bridge",
    version="0.0.1",
    description="Stable Frappe methods for employee agent workflows.",
    packages=find_packages(),
    include_package_data=True,
)
