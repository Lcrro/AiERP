# 小型土木公司一日运转沙盘数据

这是 `data/scenario/` 的目标整理位置。当前报告仍保留在旧目录以保持文档和脚本兼容；后续迁移时按下面规则分区。

| 子目录 | 内容 |
|---|---|
| `seed/` | 初始化基础数据的输入和输出报告。 |
| `runs/` | dry-run 或 apply 模式的一日事件流运行报告。 |
| `smoke/` | 单条链路 smoke 报告，例如 MR -> PO、PO -> PR、PR -> PI。 |

沙盘数据只用于业务验收和开发验证，不作为生产业务逻辑来源。
